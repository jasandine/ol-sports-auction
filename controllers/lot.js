Olsport.LotController = Ember.ObjectController.extend({
  actions: {
    // delete: function(lot) {
    //   var lot = this.get('model');
    //   var auction = this.get('model');
    //   debugger;
    //   auction.removeObject(lot);
    //   lot.destroyRecord();
    // }
  }
});
